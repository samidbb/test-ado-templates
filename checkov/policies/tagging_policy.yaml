---
metadata:
  name: "Check that all resources are tagged with the valid keys"
  id: "CUSTOM_TAGS_POLICY"
  category: "Best Practices"
  guideline: "https://wiki.dfds.cloud/en/teams/tagging_guide"
definition:
  # cond_type: "attribute"
  # resource_types: "all"
  # attribute: "tags.dfds.env"
  # operator: "exists"
  # and:
  cond_type: "attribute"
  resource_types: "all"
  attribute: "tags.dfds.env"
  operator: "subset"
  value:
    - prod
    - dev
